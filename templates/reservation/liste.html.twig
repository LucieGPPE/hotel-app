{% extends 'base.html.twig' %} {% block title %}Hello ContactController!{%
endblock %} {% block body %}
<h1>Vos réservations :</h1>
{% for reservation in reservations %}
<p>{{ reservation.suiteId.titre }}</p>
<p>{{ reservation.suiteId.prix }}</p>
<p>Date début : {{ reservation.dateDebut|date('d-m-Y')}}</p>
<p>Date fin : {{ reservation.dateFin|date('d-m-Y') }}</p>

{% endfor %} {% endblock %}
